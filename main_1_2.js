// arrow function
// print all numbers in an array

const numbArrow = (arr) => {
    for (let i = 0; i < arr.length; i++) {
        console.log(arr[i]);
    }
} ;

const numbers = [1, 2, 3, 4, 5];
numbArrow(numbers);