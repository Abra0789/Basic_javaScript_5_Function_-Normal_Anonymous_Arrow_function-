// normal function
// use loop and show all array elements in console
function numb(arr) {
    for (let i = 0; i < arr.length; i++) {
        console.log(arr[i]);//arr[i] is used to access each element of the array
    }
}
const numbers = [1, 2, 3, 4, 5];
numb(numbers); 








